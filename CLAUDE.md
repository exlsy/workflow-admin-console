# CLAUDE.md

이 파일은 Claude Code (claude.ai/code)가 이 저장소의 코드를 다룰 때 참고할 수 있는 가이드를 제공합니다.

## 핵심 원칙
- 완전한 기능 단위로 작업: 작은 조각이 아닌 완전한 기능 단위로 개발합니다.
- 단순함 우선: 모든 변경은 최소한의 코드에 영향을 주도록 합니다.
- 품질 우선: 기능, 코드 품질, 성능, 보안을 동시에 고려합니다.
- 작업내용 업데이트: 진행 완료 후 md 파일을 업데이트하고 작업 내역을 기술합니다.
- 중요사항** Claude 계획 모드에서는 md 파일만 작성하고 실제 코드 작성을 진행하지 않습니다.
- 자동 승인 모드 on**: `auto-accept edits on` 모드에서는 bash의 grep 명령이 사용자 확인 없이 바로 실행됩니다.
- 한글: 사용자와의 모든 대화는 한글로 진행합니다. **한자(漢字)는 사용 금지** — 우선순위 등도 高/中/低 대신 높음/보통/낮음으로 표기합니다.
- **모드 변경 금지 (필수)**: 계획 모드(Plan Mode) ↔ 일반 모드 전환은 **절대로** 자동으로 수행하지 않습니다. `EnterPlanMode`, `ExitPlanMode` 도구는 사용자가 명시적으로 "/plan" 또는 모드 전환을 요청할 때만 사용합니다. 사용자가 "구현해", "수정해" 등의 지시를 해도 자동으로 계획 모드로 전환하지 않습니다.
- **임시방안 금지 (필수)**: 임시방안(workaround), 우회 처리, "나중에 개선" 코드는 절대 작성하지 않습니다. 항상 근본 원인을 해결하는 정석 방식으로 구현합니다. 데이터 정합성 문제는 소스에서 해결하고, 소비 측에서 보정하지 않습니다.
- **테스트 실패 회피 금지 (필수)**: 테스트 실행 중 실패가 나오면 **절대 `--ignore` / `skip` / "pre-existing" / "별도 시스템" 같은 이유로 제외하지 않습니다**. 같은 저장소는 같은 시스템입니다. 다음 둘 중 하나로 처리합니다:
  - (A) **근본 해결**: 테스트 또는 코드의 오류를 v3 기준으로 수정
  - (B) **완전 제거**: 해당 테스트가 v2 레거시 개념(ExchangeDataState, HeikinAshi, OI/Funding, BTCUSDT 심볼 등)에 의존하여 v3 에서 의미를 잃었다면 파일째 삭제
  - 근거가 불분명하면 사용자에게 확인. 절대 "이 세션 범위 밖"이라는 이유로 회피 금지.
- **세션 범위 판단 금지 (필수)**: "이번 세션의 범위를 넘어선다", "별도 세션에서 다뤄야 한다" 등 세션 범위에 대한 판단을 절대 하지 않습니다. 세션의 범위와 진행 여부는 오직 사용자가 결정합니다.
- **추측성 답변 자제 (필수)**: 코드/데이터/문서를 직접 확인하지 않은 채 "~일 것이다", "~때문일 가능성", "보통 ~이니까" 같은 추측으로 답하지 않습니다. 다음 원칙을 따릅니다:
  - **확인 후 답**: 답하기 전에 관련 코드/로그/문서를 직접 조회하여 사실로 검증합니다. 짧은 샘플(예: 30분 로그)만 보고 패턴을 단정하지 않습니다.
  - **모르면 모른다고 답**: 검증 수단이 없거나 정보가 부족하면 추측으로 메우지 말고 "확인 필요" 또는 "모름" 으로 답하고 어떤 정보가 더 필요한지 명시합니다.
  - **여러 해석 가능 시 옵션 제시**: 데이터가 여러 해석을 허용하면 단정하지 말고 가능한 해석을 옵션으로 나열하고 사용자에게 추가 정보를 요청합니다.
- **세션 파일 기록 형식 (필수)**: 세션 파일(`.claude/sessions/*.md`) 및 일반 문서(`docs/**/*.md`) 에 작업 내역을 기록할 때는 **요약 + 헤더/불릿 리스트 위주**로 작성합니다. 코드 레벨 상세는 절대 기록하지 않습니다.
  - **마크다운 테이블(`|` + `---`) 사용 금지 (필수)**: 사용자의 마크다운 뷰어가 GFM 테이블을 렌더링하지 못해 원시 `|` 문자가 그대로 보여 가독성을 완전히 무너뜨립니다. 모든 표 형식의 정보는 **헤더(`###`) + 불릿 리스트(`-`)** 또는 **볼드 라벨 + 콜론** 형식으로 풀어씁니다.
  - **금지**: 함수/메서드 시그니처, 파라미터 이름, 라인 번호, 내부 알고리즘/메커니즘 상세 설명, 복잡도 분석, prop 이름, 변수명, dict 키 목록, SQL 쿼리 내용 등 코드 레벨 디테일. 구현은 Claude 가 맡고 사용자는 흐름과 결과만 추적함.
  - **소스코드 구현/변경 내역은 무조건 요약 (필수)**: 각 수정 항목을 개별 항목으로 나열하거나 단계별로 풀어쓰지 말 것. 사용자는 구현 디테일을 추적하기 원하지 않으며, 너무 많은 내용은 전체 흐름 파악을 방해함.
    - **금지 예**: "1층 seal_if_expired 추가", "1층 broker time 획득", "1층 DI 주입", "1층 단위 테스트", "2층 DB 스캔 메서드" 등 구현 단위마다 개별 항목으로 나열
    - **허용 예**: Phase별로 "Time-based sealing 2층 구조(운영 중 빌더 seal + startup DB 스캔) 구현 및 단위 테스트 추가" 식으로 **1~3줄로 압축**
  - **허용**: Phase별 1~3줄 요약, 항목 리스트(상태 트래킹 — 구현 세부가 아니라 조사/의사결정 흐름), 변경 파일 리스트(파일명 + 한 줄 변경 요약), 미해결 이슈 리스트, 결정 사항(채택/폐기 사유 한 줄)
  - **Phase 순서 유지**: Phase 번호는 반드시 시간 순서대로 추가합니다(Phase 4 다음엔 Phase 4.1 → 4.2 → 5 식). 중간에 끼워넣지 않습니다.

## SOLID 원칙 적용
- 단일 책임 원칙(SRP): 각 컴포넌트와 함수는 하나의 책임만 가집니다.
- 개방-폐쇄 원칙(OCP): 새로운 기능을 추가할 때 기존 코드를 수정하지 않고 확장할 수 있도록 설계합니다.
- 리스코프 치환 원칙(LSP): 상위 타입의 객체를 하위 타입의 객체로 대체해도 프로그램의 정확성이 유지되도록 합니다.
- 인터페이스 분리 원칙(ISP): 클라이언트가 사용하지 않는 인터페이스에 의존하지 않도록 합니다.
- 의존성 역전 원칙(DIP): 고수준 모듈이 저수준 모듈에 의존하지 않도록 추상화에 의존합니다.

## 작업 프로세스

### 1. 사전 개발 요구사항
항상 다음을 수행합니다:
- 전체 코드베이스를 읽고 이해합니다.
- 구체적인 TODO 체크리스트를 작성합니다.
- 변경 대상 파일의 범위를 정의합니다.
- 기존에 구현된 기능과 중복기능인지 반드시 확인합니다.
- 기존에 구현된 기능이 있다면 동일한 기능을 중복구현하지 않아야 한다. 

### 2. 기획 단계
- [ ] 명확한 기능 정의
- [ ] 단계별 TODO 리스트  
- [ ] 예상 파일 변경 범위
- [ ] 잠재적 위험 요소
- [ ] 성공 기준 정의

코딩 전 반드시 사람의 승인을 기다립니다.

### 3. 구현 단계
각 TODO에 대해 다음을 수행합니다:
- 완전한 기능 단위로 구현합니다.
- 파일 변경은 최소화합니다.
- 즉시 동작 가능한 코드를 작성합니다.
- 상세 로그를 적극적으로 작성합니다:
  * 함수 진입/종료 지점
  * 주요 변수 값 변경
  * API 요청/응답
  * 잠재적 오류 지점
  * 조건 분기 결과
- 상위 수준에서 변경 요약을 제공합니다.
- 해당 TODO 항목에 완료 표시를 합니다.

### 4. 품질 확인
항상 다음을 확인합니다:
- 코드 품질 개선 기회
- 성능 최적화 가능성  
- 보안 취약점 존재 여부
- 테스트 필요 여부

### 5. 문제 해결
오류 발생 시:
- 우선 기존 로그 출력을 확인하고 분석합니다.
- 필요 시 로그를 보강합니다:
  * 문제의 5가지 근본 원인 분석
  * 각 원인별 식별 로그 생성
  * 더 세분화된 디버깅 포인트 추가
- 로그 결과로 정확한 위치를 파악합니다.
- 원인 제거 방식을 단계적으로 적용합니다.


### 6. 완료 단계
완료되면:
- 모든 변경된 코드를 리뷰합니다.
- 원래 계획과 비교합니다.
  * 구현된 변경사항 요약
  * 발견된 문제 및 해결 방법
  * 향후 개선 방향
  * 관련 파일 태그

## 금지사항

### 절대 하지 말아야 할 것
절대 하지 마세요:
- 대규모/복잡한 변경
- 의도하지 않은 결과를 그대로 둠
- 문맥 과부하 발생
- 나쁜 코드 확산
- 계획 없이 즉흥적 코딩

### 즉시 중단해야 할 상황
다음이 감지되면 즉시 중단하세요:
- 계획과 다른 방향으로 진행 중
- 코드가 복잡해짐
- 오류 원인을 이해하지 못함
- 변경 범위가 예상보다 큼

## 프로젝트 현황

현재 이 저장소에는 `docs/프로젝트_목표.md` 스펙 문서만 있습니다. 아직 코드, `package.json`, `git` 이력이 없습니다. 스펙은 **WorkFlow Admin Console**(사내 업무 요청 관리 콘솔)을 만드는 2주 학습 코스를 정의합니다. 이 프로젝트의 의도적 목표는 최신 도구를 쓰는 것이 아니라 *구형* 엔터프라이즈 React 스택에 적응(기존 코드 읽기/수정/기능 추가)하는 것입니다. 스캐폴딩할 때는 현대적 기본값을 쓰지 말고 아래의 스펙 스택과 구조를 따르세요.

## 목표 스택 (현대화하지 말 것)

스펙은 TypeScript, Vite, React 18/19, Next.js, Zustand, TanStack Query, React Hook Form, Zod, React Router v6/v7을 명시적으로 제외합니다. 다음을 사용하세요:

- **React 16.14** — `ReactDOM.render` 사용 (`createRoot` 아님), JavaScript (TS 아님)
- **Webpack 4** (`webpack-cli@3`, `webpack-dev-server@3`) + **Babel 7** (`@babel/preset-env`, `@babel/preset-react`) — 직접 설정, CRA/Vite 사용 안 함
- **react-router-dom v5** (`Switch`, `Redirect`, `component={}` prop, `useParams`)
- **antd** — 버전이 중요함 (아래 주의사항 참고)
- **Classic Redux** (`redux` + `react-redux`) + **redux-thunk** — `actions.js` / `reducer.js` / `selectors.js`를 직접 작성, Redux Toolkit 아님
- HTTP는 **axios**, Mock API는 **json-server**

## 명령어

스펙의 `package.json` scripts 기준 (스캐폴딩 시 생성):

```bash
npm start          # webpack-dev-server, :3000 포트, /api -> :4000 프록시
npm run build      # cross-env NODE_ENV=production webpack --mode production -> dist/
npm run mock       # json-server --watch db.json --port 4000
```

플랜에 테스트 러너는 명시되어 있지 않습니다. 코스 범위가 Vitest를 제외하므로, 추가하기 전에 먼저 확인하세요.

## 아키텍처

`src/` 하위의 기능(feature) 기반 구조 (스펙 §3의 목표 레이아웃):

- `src/index.jsx` — 진입점; `ReactDOM.render(<App/>, #root)`
- `src/App.jsx` — `Provider`(Redux store) + `BrowserRouter`로 감쌈
- `src/store/` — `configureStore.js`(redux-thunk 적용), `rootReducer.js`
- `src/routes/` — `AppRouter.jsx`(v5 `Switch`), `PrivateRoute.jsx`(인증 게이트)
- `src/features/<name>/` — 각 기능이 페이지 + Redux 모듈(`actions.js`, `reducer.js`, `selectors.js`)을 함께 배치. 기능: `auth`, `dashboard`, `requests`
- `src/components/common/` 및 `src/components/layout/` — 공용 프레젠테이션 컴포넌트 (`AppLayout`, `Sidebar`, `StatusTag` 등)
- `src/api/` — `client.js`(axios 인스턴스, `baseURL: '/api'`)와 도메인별 `requestApi.js` / `authApi.js`

Webpack 설정은 `@` -> `src` import alias와 클라이언트 사이드 라우팅을 위한 `historyApiFallback`을 제공합니다. 개발 서버는 `/api`를 `:4000`의 json-server로 프록시합니다.

### Redux 비동기 패턴

Thunk는 3-액션 컨벤션을 따릅니다: `FETCH_*_START` → `FETCH_*_SUCCESS` → `FETCH_*_FAILURE`. loading/error 상태는 reducer에서 관리합니다. Reducer는 불변성을 지켜 업데이트해야 합니다 (직접 수정 금지, spread 사용).

### 업무 요청 도메인 모델

요청은 `REQUESTED → IN_PROGRESS → APPROVED / REJECTED → DONE` 상태로 전이합니다. 각 요청은 처리 이벤트의 `history` 배열을 가집니다. 사용자는 버튼 노출을 제어하는 `role`(예: `ADMIN`)을 가집니다. 라우트: `/login`, `/dashboard`, `/requests`, `/requests/new`, `/requests/:id`, 그리고 404.

## 중요 주의사항: antd 버전

antd v3와 v4는 Form API가 호환되지 않으며 이 저장소는 둘 중 어느 쪽이든 대상이 될 수 있습니다. **Form 코드를 작성하기 전에 설치된 antd 버전을 반드시 확인하세요:**

- **v3**: `Form.create()` HOC + `getFieldDecorator('field', {...})(<Input/>)`
- **v4**: `Form.useForm()` + `<Form.Item name="..." rules={[...]}>` + `onFinish`

둘을 섞으면 동작하지 않습니다. 일반적으로도 마찬가지입니다: 구형 코드베이스이므로 편집 시 최신 관용구로 재작성하지 말고 기존 패턴(class component, hooks보다 `connect()`/`mapStateToProps`)을 따르세요.
